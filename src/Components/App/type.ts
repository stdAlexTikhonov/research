export type Props = {
  name: string | undefined;
  version: string | undefined;
  env: string | undefined;
  showHeader?: boolean;
};

export type ListItemProp = {
  code: string;
  caption: string;
};
